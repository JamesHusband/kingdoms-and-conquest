export function HUD() {
  return (
    <div
      style={{
        position: "absolute",
        top: 12,
        left: 12,
        padding: 8,
        background: "rgba(0,0,0,0.5)",
        color: "#fff",
        borderRadius: 6,
      }}
    >
      Kingdoms & Conquest — DEV
    </div>
  );
}
